.DS_Store
public/
.chum-cache/

# Local Obsidian app state. Contains plugin credentials and large binaries;
# the site build does not need it.
.obsidian/

# Smart Connections plugin embeddings cache.
.smart-env/

.quartz/node_modules/
.quartz/.quartz-cache/
.quartz/.quartz/
.quartz/plugins/

# Course reading PDFs. Large (79MB) and mostly not ours to redistribute;
# kept locally and out of the published repo for now.
*.pdf

# Exceptions: the two Montfort chapters linked from the schedule.
!content/pdf/nick-montfort-adventure.pdf
!content/pdf/nick-montfort-zork.pdf

# Editor state. Local settings and appearance, not site content; the same
# reasoning that keeps .obsidian/ out.
.antigravity/
.cursor/
.vscode/
.windsurf/

# Blackboard push artifacts. Regenerated by script/blackboard on every run, the
# same way public/ is by the build; blackboard.json (the course config) is tracked.
blackboard-*.html
